# Soma host-side dependencies.
# The model inside the sandbox container installs its own packages on demand
# (pypdf, extract_msg, beautifulsoup4, requests, ...) — those are NOT listed here.

aiogram>=3.0,<4.0
httpx>=0.27
pyyaml>=6.0
